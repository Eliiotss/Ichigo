# Builds and tests the Ichigo iOS app package.
#
# Ichigo is a SwiftUI iOS application defined as a Swift Playgrounds / Xcode app
# package (see Package.swift). Because its sources depend on the iOS SDK
# (SwiftUI / UIKit), it must be built with `xcodebuild` against an iOS Simulator
# destination — the plain `swift build` command targets the host platform and
# cannot compile UIKit.
#
# The simulator device / OS names below match the Xcode version pinned in the
# "Select Xcode" step; update them together if the runner image changes.

name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build & Test (iOS Simulator)
    runs-on: macos-14

    env:
      SCHEME: Ichigo
      DESTINATION: "platform=iOS Simulator,name=iPhone 15"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode 15
        run: sudo xcode-select -s /Applications/Xcode_15.4.app

      - name: Environment
        run: |
          xcodebuild -version
          swift --version

      # `-sdk iphonesimulator` pins every target (including the AppFeature
      # library and the test bundle) to the iOS Simulator SDK. Without it,
      # xcodebuild can fall back to building for "My Mac", which fails because
      # the code is iOS-only (UIKit / SwiftUI availability).
      - name: Build
        run: |
          set -o pipefail
          xcodebuild build \
            -scheme "$SCHEME" \
            -sdk iphonesimulator \
            -destination "$DESTINATION" \
            -skipPackagePluginValidation \
            CODE_SIGNING_ALLOWED=NO

      - name: Test
        run: |
          set -o pipefail
          xcodebuild test \
            -scheme "$SCHEME" \
            -sdk iphonesimulator \
            -destination "$DESTINATION" \
            -skipPackagePluginValidation \
            CODE_SIGNING_ALLOWED=NO
