package com.ichigo.app.ui.theme

import androidx.compose.ui.unit.dp

/**
 * Sizes from `AppTheme.swift` (points → dp, one-to-one). Used for card/hero/tile
 * corner radii and the bottom padding of tabbed screens.
 */
object Dimens {
    val CardRadius = 22.dp
    val HeroRadius = 26.dp
    val TileIconRadius = 15.dp
    val ScreenBottomPadding = 28.dp
}
